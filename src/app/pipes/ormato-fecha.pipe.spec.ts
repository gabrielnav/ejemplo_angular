import { OrmatoFechaPipe } from './ormato-fecha.pipe';

describe('OrmatoFechaPipe', () => {
  it('create an instance', () => {
    const pipe = new OrmatoFechaPipe();
    expect(pipe).toBeTruthy();
  });
});
