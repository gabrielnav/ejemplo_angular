import { booleanoEstiloDirective } from './booleano-estilo.directive';

describe('booleanoEstiloDirective', () => {
  it('should create an instance', () => {
    const directive = new booleanoEstiloDirective();
    expect(directive).toBeTruthy();
  });
});
